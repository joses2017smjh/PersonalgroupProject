<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <ImageView
        android:id="@+id/icon_restroom"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentStart="true"
        android:layout_marginStart="3dp"
        android:src="@drawable/ic_restroom" />

    <TextView
        android:id="@+id/text_building_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="-1dp"
        android:layout_toEndOf="@id/icon_restroom"
        android:text="Building Name"
        android:textColor="#000"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/text_distance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/text_building_name"
        android:layout_marginStart="10dp"
        android:layout_marginTop="25dp"
        android:layout_toEndOf="@id/icon_restroom"
        android:text="100 ft away"
        android:textColor="#666"
        android:textSize="14sp" />

    <RatingBar
        android:id="@+id/bathroom_rating_stars"
        android:layout_width="wrap_content"
        android:layout_height="42dp"
        android:layout_marginEnd="6dp"
        android:layout_toStartOf="@id/icon_arrow"
        android:isIndicator="true"
        android:numStars="4"
        android:rating="3.5"
        android:scaleX="0.8"
        android:stepSize="0.5" />

    <ImageView
        android:id="@+id/icon_arrow"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="1dp"
        android:src="@drawable/ic_arrow" />
</RelativeLayout>
